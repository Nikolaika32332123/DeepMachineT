# DeepMachineT: Энергоэффективный ML-фреймворк на C++

**DeepMachineT** — это высокопроизводительный фреймворк машинного обучения, написанный на **чистом C++** с Python-биндингами. Система разработана для исследования альтернативных парадигм обучения, отказываясь от классического обратного распространения ошибки в пользу **локального обучения с подкреплением** и **целочисленной арифметики**.

Главная идея: **максимальная эффективность** через отказ от дорогостоящих операций с плавающей точкой и использование только целочисленной логики, что делает систему идеальной для встроенных систем и критичных по энергии приложений.

---

## 🎯 Ключевые особенности

### 1. **Экологичный ИИ**
- ✅ Только **целочисленная арифметика** (`uint8` веса)
- ✅ Отсутствие умножения матриц (FLOAT операции)
- ✅ Операции: сложение, сравнение, логический AND
- ✅ **На порядки меньше энергопотребления** на CPU vs PyTorch/TensorFlow

### 2. **Биологически вдохновленное обучение**
- ✅ Локальные правила обучения (LTP/LTD) вместо глобального градиентного спуска
- ✅ **Стохастический поиск** гипотез (синаптогенез)
- ✅ **Адаптивная пластичность** через механизм "осознанности" (Deliberate mode)
- ✅ Решение проблемы **катастрофического забывания**

### 3. **Полная интерпретируемость**
- ✅ Каждое решение имеет **явный след рассуждения** — цепочку активированных нейронов
- ✅ Известна **уверенность системы** в каждый момент времени
- ✅ **Нейросимволическая архитектура** (факты, а не абстрактные признаки)

### 4. **Встроенная оптимизация модели (Pruning)**
- ✅ Автоматическое удаление **неактивных нейронов** без потери точности
- ✅ Сжатие на **50%+ по числу весов**
- ✅ Детальная статистика сокращения по слоям

### 5. **Легкая интеграция**
- ✅ C API для интеграции в любые языки (Java, C#, Go, Rust, Node.js и т.д.)
- ✅ Python wrapper через `ctypes` (без дополнительных зависимостей)
- ✅ Стандартные форматы сохранения (BIN для скорости, JSON для отладки)

---

## ⚙️ Установка и компиляция

### Требования
- **C++17** компилятор (g++, clang или MSVC)
- Поддержка **AVX2** процессором (рекомендуется)
- **Python 3.7+** (только для использования Python обертки)

### Компиляция на Windows (MinGW)

```bash
cd cpp_core
g++ -O3 -shared -fPIC -mavx2 -o ../bin/dmt_lib.dll dmt_lib.cpp
```

Или используй готовый скрипт:
```bash
.\compile_windows.bat
```

### Компиляция на Linux/macOS

```bash
cd cpp_core
g++ -O3 -shared -fPIC -mavx2 -o ../bin/libdmt.so dmt_lib.cpp
```

Или используй готовый скрипт:
```bash
chmod +x compile_linux.sh
./compile_linux.sh
```

### Проверка установки

```python
from wrappers.python.dmt_package.dmt import DMT

# Если ошибки нет — всё установлено правильно
model = DMT(depth_memory=5, num_features=5, layers=[50, 20, 2])
print("✓ DeepMachineT готов к работе!")
```

---

## 🚀 Быстрый старт

### Простой пример (Python)

```python
from dmt_package.dmt import DMT

# 1. Создание модели
model = DMT(depth_memory=5, num_features=5, layers=[50, 20, 2])

# 2. Подготовка данных (бинарные вектора 0 и 1)
X_train = [
    [1, 1, 1, 0, 0],  # Образец класса 0
    [0, 1, 0, 1, 1],  # Образец класса 1
    [1, 1, 0, 1, 0],  # Образец класса 0
    [0, 1, 1, 0, 1],  # Образец класса 1
]
y_train = [0, 1, 0, 1]

# 3. Обучение (локальное обучение, БЕЗ градиентного спуска)
model.learn_sl(X_train, y_train, epochs=300, verbose=True)

# 4. Оценка точности
accuracy = model.evaluate(X_train, y_train)
print(f"Точность: {accuracy:.2f}%")

# 5. Предсказание
prediction = model.predict([0, 0, 1, 1, 1])
print(f"Предсказание: {prediction}")

# 6. Сохранение модели
model.save("my_model.bin")
```

### Использование из других языков

DeepMachineT имеет **C API** (файл `cpp_core/include/dmt_lib.h`), поэтому может быть интегрирована в:

**Java / Kotlin (JNI):**
```java
System.loadLibrary("dmt_lib");
// ... вызов C функций
```

**C# / .NET (P/Invoke):**
```csharp
[DllImport("dmt_lib.dll")]
public static extern IntPtr DMT_Create(...);
```

**Go (cgo):**
```go
// #cgo LDFLAGS: -L./bin -ldmt_lib
import "C"
```

---

## 📚 API документация (Python)

### Основной класс: `DMT`

#### Инициализация
```python
model = DMT(depth_memory=5, num_features=5, layers=[50, 20, 2])
```

| Параметр | Тип | Описание |
|----------|-----|---------|
| `depth_memory` | int | Максимальная "глубина памяти" синапса (1-255). Максимальный вес связи. |
| `num_features` | int | Размер входного вектора (число признаков) |
| `layers` | List[int] | Архитектура сети. Пример: `[50, 20, 2]` = 50→20→2 нейронов |

#### Методы обучения

**`learn_sl(X, y, epochs=100, verbose=True)`** — Обучение с учителем
- `X`: Обучающие данные (List[List[int]])
- `y`: Метки классов (List[int])
- `epochs`: Число эпох
- `verbose`: Показывать ли прогресс-бар

**`learn_rl(x, reward)`** — Обучение с подкреплением
- `x`: Входной образец (List[int])
- `reward`: +1 (верно) или -1 (неверно)

#### Основные методы

**`predict(x) -> int`** — Предсказание класса
**`evaluate(X, y) -> float`** — Оценка точности (в процентах)
**`save(path, as_json=False)`** — Сохранение весов
**`load(path)`** — Загрузка весов (оба формата BIN и JSON)
**`compress_pruning(save_path)`** — Сжатие через удаление неактивных нейронов

Примеры использования см. в `wrappers/python/main.py`.

---

## 🧠 Архитектура и принципы работы

### Три состояния нейрона

| Состояние | Вес | Описание |
|-----------|-----|---------|
| **Пустота** | w=0 | Связь не сформирована |
| **Гипотеза** | w=1 | Неустойчивая связь (в поиске) |
| **Знание** | w>1 | Консолидированная связь (в использовании) |

### Обучение: LTP и LTD

**LTP (Усиление):** Если ответ верен → вес увеличивается (w ← w+1)  
**LTD (Ослабление):** Если ответ неверен → вес уменьшается (w ← w-1)  
**Защита знаний:** Уверенные нейроны защищены от обновления при ошибках в других слоях

### Механизм "Осознанности"

Система отслеживает свою уверенность:
- **Deliberate = 0**: Все веса ≥ 1 → уверена → обучение замедляется
- **Deliberate = 1**: Есть вес = 1 → сомневается → обучение ускоряется

Это решает проблему **катастрофического забывания** и позволяет сети адаптировать скорость обучения.

---

## 📊 Экспериментальные результаты

| Dataset | DeepMachineT | MLP | Random Forest | SVM |
|---------|--------------|-----|---------------|-----|
| **Iris** | 93.33% | 93.33% | 94.67% | 95.33% |
| **Wine Quality** | 92.70% | 96.08% | 96.63% | 97.76% |
| **Breast Cancer** | 93.33% | 95.26% | 94.20% | 93.15% |

**Ключевые наблюдения:**
- Точность **сравнима с классическими методами**, хотя используется только бинарный сигнал подкрепления
- На медицинских данных показывает **высокую стабильность**
- **На порядки меньше энергопотребления** за счет целочисленной арифметики

---

## 🔬 Научная основа

DeepMachineT вдохновлена идеями:
- **М.М. Бонгарда** о построении концепций
- **K. Friston's Free Energy Principle** — минимизация локальной энергии
- **Synaptic Plasticity** — LTP/LTD в биологии
- **Markov Blankets** — локальность в нейронных сетях

**Подробное описание теории и экспериментов:** см. `docs/DeepMachineT_Paper_RU.pdf`

---

## 🚀 Будущие разработки

### Графический редактор нейросетей (In Progress)

Одна из главных фишек DeepMachineT — **нейросимволическая архитектура**, которая делает сеть визуально интерпретируемой и редактируемой. В планах разработка:

- **Visual Network Editor** — графическое приложение для визуального конструирования нейросетей
- **AI Assistant** — встроенный ассистент, который поможет проектировать сети и объяснять решения

### Проект открыт для идей! Возможные направления развития

- **Распределенное обучение** — синхронизация весов между нейронами (Gossip algoritm)
- **Многомодальность** — интеграция разных типов данных (текст, изображения, звук)
- **Расширение языков** — адаптация под языки программирования: C#, Java, JavaScript

---

## 📝 Лицензия

MIT License — свободно используйте в коммерческих и личных проектах.

---

## 📧 Вопросы и обратная связь

**Автор:** Николай Токарев  
**Email:** nikolaos@byte-agi.ru  

Если у вас есть вопросы, идеи или вы нашли баг — пишите!

---

## 📖 Дополнительные ресурсы

- `cpp_core/dmt_lib.cpp` — Исходный код ядра на C++
- `demo_pure_python/DeepMachineT.py` — Простая Python версия для обучения
- `wrappers/python/main.py` — Полные примеры всех функций
- `docs/DeepMachineT_Paper_RU.pdf` — Статьи с теорией и экспериментами

---

**Последнее обновление:** 10 Декабря 2025  
**Версия:** 1.0-beta  
**Статус:** Активная разработка